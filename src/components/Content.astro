---
const { lang = 'en' } = Astro.props;
import { t } from '../i18n';
---

<section class="text-center p-8">
    <h1 class="text-4xl font-bold">{t(lang, 'hero.title')}</h1>
    <p>{t(lang, 'hero.download')}</p>
</section>
